<div class="container d-flex justify-content-center pt-5 pb-3">
  <p class="h2">Здравствуйте <%= current_user.name if current_user.present? %> !!!</p>
</div>
<div class="d-flex justify-content-center">
  <p>
    <strong>Avatar:</strong>
  </p>
  <%= image_tag user_avatar(@user) %>

  <p>
    <strong>Name:</strong>
    <%= @user.name %>
  </p>

  <p>
    <strong>Email:</strong>
    <%= @user.email %>
  </p>
</div>

<td><%= link_to 'Edit', edit_user_registration_path %></td>
<%= link_to 'Back', root_path %>