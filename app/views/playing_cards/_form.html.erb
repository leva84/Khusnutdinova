<%= form_with(model: playing_card, local: true) do |form| %>
  <% if playing_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playing_card.errors.count, "error") %> prohibited this playing_card from being saved:</h2>

      <ul>
      <% playing_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
